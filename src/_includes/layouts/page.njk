{% extends 'layouts/base.njk' %}

{% block content %}
    <main>
        <article class="page">
            <header>
                <h1>{{ title }}</h1>
            </header>
            {{ content | safe }}
        </article>
    </main>
{% endblock %}

{{ content | safe }}
