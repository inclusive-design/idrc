{% extends 'layouts/base.njk' %}

{% set pageType = 'page--offline' %}

{% block content %}
    <article>
        {% include 'partials/components/page-header.njk' %}
        <div class="content">
            <div class="inner">
            {{ content | safe }}
            </div>
        </div>
    </article>
{% endblock %}

{{ content | safe }}
