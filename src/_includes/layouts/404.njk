{% extends 'layouts/base.njk' %}

{% set pageType = 'page--404' %}

{% block content %}
    <article>
        {% include 'partials/components/page-header.njk' %}
        <div class="content">
            <div class="wrapper">
            {{ content | safe }}
            </div>
        </div>
    </article>
{% endblock %}

{{ content | safe }}
